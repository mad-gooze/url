doctype html
html
    head
        meta(charset='utf-8')
        meta(name='viewport', content='width=device-width, initial-scale=1.0')
        title Redirect
        script.
            window.onload = function () {
                window.location.replace("!{url}");
            }
    body
        h1 Redirecting to #[a(href=url)= url]
        if (metrikaID)
            img(src='//mc.yandex.ru/watch/41179714')

